app:
  name: "linkdead"
  version: "v1"
  mode: "development"

server:
  port: 5000
  trustProxy: false
  allowedCorsOrigins:
    - "*"
  apiPrefix: "dev:api"
  docsPrefix: "dev:docs"

logger:
  level: "verbose"
  dir: "./logs"
  activeFiles: false

